/**
 * 
 * @param {*} points 
 * @param {*} homework 
 * @param {*} totalHomework 
 */
function htmlGrade(points, homework, totalHomework) {
    let a = 0;
    let b = [];
    b.
}
htmlGrade(300, 10, 10)