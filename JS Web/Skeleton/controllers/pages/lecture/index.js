const { renderAttachLecture, createLecture, attachLecture, deleteLecture, } = require('./lecture')

module.exports = {
    renderAttachLecture,
    createLecture,
    attachLecture,
    deleteLecture,
}